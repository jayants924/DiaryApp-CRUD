@model DiaryEntry

<div class="container">
    <div class="row">
        <div class="col">
            <form asp-action="Delete" method="post">
                <fieldset>
                    <legend>Delete Diary Entry</legend>
                    <div>
                        <label asp-for="Title" class="form-label mt-4">Title</label>
                        <input asp-for="Title" type="text" class="form-control" id="title" placeholder="Enter a title" readonly>
          
                    </div>
                    <div>
                        <label asp-for="Content" class="form-label mt-4">Content</label>
                        <textarea asp-for="Content" class="form-control" id="content" rows="2" placeholder="Enter ehat you did that day.." readonly></textarea>
                      
                    </div>
                    <div>
                        <label asp-for="Created_at" class="form-label mt-4">Date</label>
                        <input asp-for="Created_at" type="date" class="form-control" placeholder="Enter a date" readonly>
                 
                    </div>
                    <br />
                    <button type="submit" class="btn btn-danger">Delete</button>
                    <a class="btn btn-default" asp-area="" asp-controller="DiaryEntries" asp-action="Index">Cancel</a>
                </fieldset>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

